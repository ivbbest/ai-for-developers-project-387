# Как изучать проект «Календарь звонков (продолжение)» (путеводитель)

Статус: актуален на 2026-09-04 (шаг 1 — перенос кода — не выполнен; помечено,
что появится после него)
Зачем: быстро погрузиться — понять, что здесь предмет проекта (процесс, не
продукт), где мы сейчас, как выполнять шаги. Маршрут покрывает: что было в
репозитории изначально, входное задание, решения и план, память проекта.

Главное отличие от предшественника (386): там строили приложение, здесь
**встраивают агента в GitHub-процесс** готового приложения. Проверка результата —
не только зелёные тесты, а **видимый след**: прогон Actions, ответ агента в
issue, коммиты в PR, артефакт отчёта.

---

## Порядок чтения (от общего к частному)

### Блок A — «Что здесь было» (каркас курса и основы)

| Шаг | Что читать | Что это, зачем |
|---|---|---|
| A1 | `README.md` (корень) | Визитка курса: «интегрируйте работу агентов в GitHub-проект»; по финальному шагу здесь появится таблица воркфлоу |
| A2 | `.github/workflows/hexlet-check.yml` + `.github/workflows/README.md` | Автотесты Хекслета на каждый коммит; `hexlet-check.yml` нельзя трогать |
| A3 | `git log --oneline` | История каркаса (пока: обновления hexlet-check). После шага 1 — перенос кода; дальше история = следы шагов 2–6 |
| A4 | `.github/workflows/` (после шагов 1–5) | Шаг 1 приносит CI-перенос (`contract-sync`, `e2e`, `docker`, `release-please`); `opencode*` (интерактив/triage/review/schedule) **создаются по одному на своих шагах 2–5** — воркфлоу активен с момента мержа, «заготовки» ломали бы следы курса |
| A5 | `.opencode/skills/` + `skills-lock.json` *(локально, вне git)* | Инструменты агента: `gh-fix-ci` (разбор красных CI), `grill-me`, `ru-text`, `supabase-postgres-best-practices` |

### Блок B — «Исходное задание» (входные материалы `input/`)

| Шаг | Что читать | Что это |
|---|---|---|
| B1 | `input/user-story.txt` | Единственный входной файл: 6 шагов (перенос+план → установка агента → issue/triage → PR/ревью → расписание → финальная проверка); у каждого — «Требования», «Задачи», «Результат шага», ссылки |
| B2 | Ссылки из B1 | opencode.ai/docs/github (события, prompt, model/mentions/share), github.com/apps/opencode-agent, docs.github.com (events), lighthouse-ci, conventionalcommits.org, release-please |

### Блок C — «Решения и план» (что добавили для работы)

| Шаг | Что читать | Что это |
|---|---|---|
| C1 | `AGENTS.md` (корень) | Правила + «Специфика курса (шаги 2–6)»: инварианты воркфлоу (types created, id-token, persist-credentials, обязательный prompt, анти-петля) |
| C2 | `.agents/tasks.md` | Подробный реестр всех 6 шагов до подпунктов: что переносить файл-в-файл, критерии-требования курса, «след» каждого шага |
| C3 | `docs/development-plan.md` *(после шага 1.4)* | План развития: ≥1 фича и ≥1 баг жалобами пользователя — источник будущих issues для шагов 3–5 |
| C4 | `docs/` после переноса *(шаг 1)* | `decision-guide.md` — архитектура и решения приложения (зачем слоты/409/TZ, как запускать); `project-understanding.md` — карта продукта; `specs/api-contract.md` — контракт |
| C5 | Предшественник `/mnt/e/hexlet/cal-com` *(локально, вне репо)* | `.agents/mem/github-integration.md` — runbook четырёх opencode-воркфлоу 386: **справочник намерений, не фактов** (по правам расходится с собственными YAML — сверять с файлами); `docs/archive/work-plan.md` — как был построен переносимый код |

### Блок D — «Память проекта» (`.agents/`, публичная курируемая, без PII)

| Шаг | Что читать | Что это |
|---|---|---|
| D1 | `.agents/context.md` | Журнал вех: дата + факт + SHA |
| D2 | `.agents/mem/MEMORY.md` | Индекс фактов (состояние, предшественник, демо-URL, ссылки) |
| D3 | `.agents/mem/learnings.md` | Грабли и уроки (append-only; ритуал конца сессии) |

---

## Минимальный путь (мало времени)

`A1` → `B1` (шапка + шаг 1) → `C1` («Специфика курса») → `C2` (первые два шага) → `D2`

## Кому что читать

- **Нужна суть проекта**: A1, B1, C1 (шапка)
- **Собираешься выполнять шаг курса**: C2 (свой шаг), C1 (специфика), C5 (runbook), D1–D3
- **Интересует приложение (что переносим)**: C4, затем код; продукт-решения — 386 `docs/decision-guide.md`
- **Интересует настройка агента в GitHub**: A4, C1, C5, следы из D1

## Маршрут изучения для того, кто будет выполнять задачи

Порядок — от «что здесь процесс» к «как выполнять»; в скобках время и
контрольный вопрос.

1. **README.md + `input/user-story.txt`** (25 мин) — прочитать все 6 шагов,
   особенно разделы «Требования».
   Вопрос: чем «тест» в этом проекте отличается от теста в 386? *(след в GitHub)*
2. **AGENTS.md** «Границы действий» + «Специфика курса» (10 мин).
   Вопрос: кто пушит и мержит; почему интерактив не требует prompt, а triage —
   требует?
3. **`.agents/tasks.md`** — шаг 1 целиком (15 мин).
   Вопрос: что именно переносится и чего переносить нельзя? *(node_modules,
   dist, backend/data, секреты; hexlet-check не трогать)*
4. **Предшественник: `docs/specs/api-contract.md` + 5 ручек** (20 мин) —
   достаточно понять контракт и сценарий гостя/владельца: агент в шагах 3–4
   будет разбирать жалобы про это приложение.
   Вопрос: какой код вернёт повторная бронь занятого слота? *(409 slot_conflict)*
5. **`/mnt/e/hexlet/cal-com/.agents/mem/github-integration.md`** (15 мин) —
   карта четырёх воркфлоу и их настройки в 386.
   Вопрос: зачем `share: false` и фильтр автора в интерактивном воркфлоу?
6. **Блок D** — состояние памяти: где сейчас (шаг 1 не начат).
   Вопрос: какой следующий незакрытый пункт `tasks.md`?

Далее — работа: берёшь первый незакрытый шаг `tasks.md`, идёшь по «Циклу
исполнения задачи» ниже. Правило курса: не начинать шаг, пока не переформулированы
его «Требования» в чекбоксы с доказательством-ссылкой.

---

## Цикл исполнения задачи (один виток = один подпункт tasks.md)

1. **Взять подпункт** из `.agents/tasks.md`; прочитать его источник в
   `input/user-story.txt` (требования/задачи соответствующего шага).
2. **Выполнить** минимально: файл воркфлоу / комментарий / прогон — ровно то,
   что требует пункт, не «расширяя процесс».
3. **Верифицировать следом**: ссылка на run в Actions, URL комментария агента,
   SHA коммита в PR — не «должно работать».
4. **Локальные проверки не отменяются**: правки в репо (шаг 1, фиксы) —
   прогнать слой тестов из AGENTS.md (dev.sh-команды) до коммита.
5. **Коммит-чекпоинт** (Conventional Commits, `git diff` на секреты/PII/мусор);
   пуш/PR/мерж — пользователь.
6. **Отметить `[x]`** с датой/SHA/ссылкой; итог — в `context.md`; уроки —
   в конец `mem/learnings.md`.
7. Следующий подпункт; смена шага курса — новая ветка (и мерж предыдущей).

Правила остановки: шаг не закрывается без «следа», даже если всё «логично
настроено»; найдено противоречие между текстом курса и реальным поведением
воркфлоу — править запись в `tasks.md`/памяти с объяснением, а не обходить молча;
каждый новый интерактивный воркфлоу — сразу тест на анти-петлю (ответ агента не
должен запускать агента).

## Как ревьюить следы (вместо «ревью ветки» из 386)

```bash
gh run list --limit 20                      # прогоны: кто/что/когда запускалось
gh run view <ID> --log | head               # что делал агент в конкретном вызове
gh pr view <N> --json comments,reviews      # каналы обратной связи в PR
git log --oneline --format='%an %s' -10     # авторы коммитов: агент vs человек
```

Проверка следа: (1) событие-инициатор совпадает с замыслом шага; (2) прав у
прогона не больше, чем нужно; (3) ответ агента не породил новый прогон
(анти-петля); (4) коммиты агента — Conventional, без ИИ-формулировок.

---

## Как проверить готовность контекста (маршрут для постороннего)

Ничего не читать из чужого диалога — только репозиторий и команды. Пять шагов,
каждый с ожидаемым результатом; расхождение = находка, её не «объяснять», а
чинить в файле-источнике.

### V1. Целостность рабочего дерева (2 мин)

```bash
git status --short            # до коммита каркаса: ?? .agents/ ?? .gitignore ?? AGENTS.md ?? docs/
                              # после коммита контекста — чисто; обновить ожидание здесь
git log --oneline -3          # main = каркас курса (6c8b049 и т.п.), кода приложения ещё нет
git check-ignore -v input/ .opencode/ skills-lock.json   # все три — игнорируются
git config core.fileMode      # false (WSL-права не шумят в diff)
```

Вопрос проверяющему: почему `.agents/` при этом НЕ игнорируется? *(публичная
курируемая память, см. AGENTS.md «Что здесь лежит»).*

### V2. Факты документов против файлов (5 мин)

Каждое проверяемое утверждение — командой, не на веру:

```bash
# имена и команды workspaces, числа тестов/проверок из tasks.md §1.3
grep '"name"' ../cal-com/*/package.json ../cal-com/contract/mock-server/package.json
grep -cE '^check "|^check_body "' ../cal-com/contract/smoke.sh          # 16 + 11 = 27
grep -cE '^check "' ../cal-com/contract/mock-server/smoke.sh            # 46 (с check_body)
grep -cE '^\s*(test|it)\(' ../cal-com/backend/test/*.test.ts            # 71
# инварианты opencode-воркфлоу из шагов 2–5 курса — есть ли в переносимом шаблоне
grep -E "types: \[created\]|persist-credentials|id-token|share: false" ../cal-com/.github/workflows/opencode.yml
```

Ожидание: числа в `.agents/tasks.md` (27/46/71/9/9) совпадают; в `opencode.yml`
386 (как текст-образец) — фильтр автора, `share: false`, job-level
`timeout-minutes` (параметра `timeout` у экшена нет).

### V3. Покрытие: user-story → tasks.md (10 мин)

Механическая сверка: открыть `input/user-story.txt`, выписать пункты
разделов «Требования» всех 6 шагов (их 28) и для каждого найти чекбокс в
`.agents/tasks.md`. Ни один пункт не должен остаться без строки; ни одна строка
tasks.md не должна требовать того, чего нет в курсовом тексте (самодеятельность
допустима только помеченная, как «по желанию»/кандидаты плана).

### V4. Перекрёстная согласованность (5 мин)

- `AGENTS.md` таблица «Что здесь лежит» ↔ фактическое дерево (`ls -a`, `ls docs .agents`);
- ссылки между документами живые: `docs/development-plan.md` помечен «после шага 1.4»,
  пути в предшественник (`/mnt/e/hexlet/cal-com/...`) существуют (`ls` каждой);
- правила не противоречат друг другу: границы git в AGENTS.md ↔ «Правила ведения»
  tasks.md ↔ ритуалы в MEMORY.md (кто пушит/мержит, Conventional, анти-петля);
- память не расходится с реестром: последняя запись `context.md` описывает
  текущее состояние файлов, `tasks.md` «Настройка проекта» закрыта кроме
  строки про коммит/пуш.

### V5. Гигиена (2 мин)

```bash
grep -rniE "sk-[a-z0-9]|ghp_|xox|password\s*[:=]" AGENTS.md .agents docs   # пусто (кроме примеров-команд)
grep -rn "email\|телефон" .agents/mem/MEMORY.md                            # нет PII
grep -rn "386" README.md                                                   # README ссылается только на 387
```

### Итоговый след

После коммита контекста: зелёный `hexlet-check` в Actions нового репо
(ссылка на run) — это единственный «тест», которым подтверждается, что каркас
не сломал автотесты курса.

