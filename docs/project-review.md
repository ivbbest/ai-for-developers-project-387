# Ревью проекта: состояние и рекомендации

Дата ревью: 2026-09-05. Объект: репозиторий
[ivbbest/ai-for-developers-project-387](https://github.com/ivbbest/ai-for-developers-project-387),
ветка `main` (`8f3e074`), локальная копия рабочего каталога проекта.

## Итог

Проект в рабочем состоянии: шаги 1–5 курса закрыты и подтверждены следами в
GitHub (прогоны, issue, PR, релиз), шаг 6 подходит к концу (пункты 6.1–6.9
закрыты). Критических дефектов не найдено. Есть один **временной блокатор** —
слияние PR #30 до первого ночного запуска по расписанию, — и несколько
рекомендаций по устойчивости процесса, часть из них — «на потом».

## Что проверено

Проверка выполнялась по состоянию на 12:20 UTC 2026-09-05: локальный `git
status`/`log`/`branch`, открытые PR и issue через `gh`, список прогонов Actions,
все четыре opencode-воркфлоу построчно, README, `.gitignore`, релизы и теги,
секреты (только имена) и repository variables, следов секретов в файлах нет.

Факты, на которых строится ревью:

| Проверка | Результат |
|---|---|
| main `8f3e074`, ветка `ci/ignore-schedule-artifacts` с PR #30 (не слита) | PR #30 открыт, проверки зелёные |
| Коммиты PR #30 — один, только `.gitignore` (+5 строк) | состав корректен |
| Открытые issues на момент проверки | #6 (разведка), #12 (отмена брони), #25 (perf-report) — закрытие #6 и критерии в #12 выполнены при разборе, см. «Разбор рекомендаций» |
| Тег и релиз | `cal-com-v1.0.3` опубликован 2026-09-05 |
| Секреты | только имена `HEXLET_ID`, `OPENCODE_API_KEY`; в файлах репозитория значений нет |
| `APP_URL` | https://ai-for-developers-project-387-670a.onrender.com/ (с завершающим слэшем — воркфлоу нормализует) |
| `hexlet-check.yml` | не менялся с момента создания |
| Активные воркфлоу | 9: hexlet-check, contract-sync, e2e, docker, release-please, 4 opencode-воркфлоу |
| Упоминания предшественника 386 | только фактические (откуда перенесён код, живой issue-ссылка в спеке) |

## Состояние задач

- Шаги 1–5: закрыты, следы со ссылками в реестре (PR #2, #7, #11, #15–#17, #20,
  #22, #24–#28).
- Шаг 6: 6.1–6.9 закрыты (PR #27, #28, #29); **остаются 5.10, 6.10, 6.11**.
- Открытые PR на момент проверки: только #30 (слита до разбора, main `d085260`). Открытые issues: #6, #12, #25 (судьба #6/#12 — ниже).

## Блокаторы

1. **СНЯТ при разборе: PR #30 слита в `main` (12:20 UTC), проверки зелёные.**
   Подтверждение фактом: dispatch-прогон 33965810564 после мержа — success за
   1m54s, артефакты на месте, нового PR с отчётом экшен не открыл. Оставшийся
   риск «произвольное имя файла» закрывает Р4 (гейт чистоты каталога).
   Первый плановый запуск `opencode-schedule` — 2026-09-06 03:00 UTC
   (задача 5.10).
2. **Курс формально не закрыт до ночного прогона.** 6.10 (финальный проход по
   требованиям) и 6.11 (закрытие памяти) ждут факта 5.10. Цепочка закроется
   утром 6 сентября после проверки `gh run list … --event schedule` и
   подтверждения дедупа (комментарий в #25, а не новый issue). **Частично
   подтверждён:** ручной dispatch 12:21 UTC оставил комментарий в #25 вместо
   нового issue — дедуп работает; ночной cron всё ещё ждёт своего первого
   запуска.

## Рекомендации

### Р1. Дописать критерии приёмки в issue #12 до начала работ

В #12 есть триаж-разбор, но нет формулировки «исправлено, когда…» (это
зафиксировано как задача 4.12). Почему: без измеримой формулировки fix
не проверить объективно, а правило проекта «след = ссылка» требует проверяемого
результата; записать критерии до `/oc fix`.

### Р2. Закрыть issue #6 как разведанный

Ответ получен в issue #6 (разбор по файлам), новых вопросов нет — вопрос
закрыт. Почему: открытый issue без владельца — шум в списке; закрытие сохраняет
реестр открытых задач честным.

### Р3. Определить судьбу issue #25 (perf-report)

Цель-критерий зафиксирован («Performance ≥ 0.9, TBT < 200 мс, повторный
замер»), но работы по исправлению не назначено. Почему: цель без назначенной
задачи либо висит вечно, либо закрывается как мониторинг. Механизм «повторный
замер» уже есть — ночной crон; после двух зелёных замеров issue можно закрыть
по цели, до этого — держать открытым как источник данных.

### Р4. Страховать чистоту рабочего каталога в расписании, а не список имён

`.gitignore` закрывает три известных имени. Пост‑обработка агента может
создать произвольный файл — следующий мусорный PR будет с другим именем.
Почему: запрет в prompt не сработал дважды (факты #23, #26) — prompt задаёт
намерение, а пост‑обработку делает экшен. Надёжный гейт — шаг после
`Run opencode report`: `git status --porcelain` пуст, иначе fail с внятным
сообщением. Это фикс причины, а не симптома конкретного файла; в сочетании
с PR #30 полностью закрывает класс «агент открыл PR с отчётом».

### Р5. Убрать из текста ошибки guard'а APP_URL ссылку на внутренний реестр

Сообщение «задача 1.6 реестра» в `opencode-schedule.yml` — отсылка к
внутреннему чек‑листу проекта в публичном логе прогона. Почему: репозиторий
публичный, лог читают не только участники; текст должен быть самодостаточным
(«APP_URL не задана: Settings → Secrets and variables → Actions → Variables»).

### Р6. Добавить в интерактивный prompt требование отвечать без иноязычных вставок

В комментарии агента в PR #30 встретилась вставка «по工作机制» (символы CJK) —
артефакт модели в публичном треде. Почему: тред — витрина процесса, и одна
строка в prompt (отвечай по-русски, без вставок на других языках) закрывает
класс. Правка в `opencode.yml` — одна строка, эффект — гигиена видимой части.

### Р7. Мелочь: `issues: read` в opencode-review не используется

Ревью пишется токеном раннера (`pull-requests: write`), чтение issues не
задействовано. Почему: права держатся минимальными по шагу 6; к удалению
не срочно, но при следующей правке воркфлоу строку стоит убрать.

### Р8. Наблюдение: сдвоенный комментарий «ревью пройдено» на PR #30

Один прогон авторевью оставил два одинаковых по смыслу комментария. Prompt уже
просит «одним комментарием» — остаётся как известное ограничение модели, на
процесс не влияет.

### Р9. Наблюдение: `docs/project-context.md` называет файл правил в таблице

Строка «Правила проекта и команды CI-гейта» указывает на файл правил
локального агента — это внутренняя инфраструктура, которой в публичном
документе быть не должно. По правилам гигиены проекта такие ссылки выносятся
только в учебные planning-доки курса; заменить на нейтральное описание
(«правила локального агента»). Косметика, не блокер.

## Открытые вопросы владельцу — решены при разборе (2026-09-05)

1. PR #30 — **слита** (до этого вопроса: мерж 12:20 UTC, факт — dispatch
   33965810564 без нового PR).
2. Issue #25 — **держим как мониторинг**: открытый issue с меткой `perf-report`
   обновляется ночными прогонами (дедуп подтверждён комментарием 12:23 UTC);
   закрытие — по достижении цели (Performance ≥ 0.9, TBT < 200 мс) или по
   решению владельца назначить работу отдельно.
3. Issue #6 — **закрыт** (разведка выполнена, ответ получен, открытых вопросов
   нет). Issue #12 — **остаётся открытым**, критерии приёмки дописаны (Р1);
   `/oc fix` по нему — после закрытия цепочки шага 6.

## Разбор рекомендаций

| Пункт | Решение | След |
|---|---|---|
| Р1 | выполнена | [критерии приёмки в #12](https://github.com/ivbbest/ai-for-developers-project-387/issues/12#issuecomment-5551839979) |
| Р2 | выполнена | [#6 закрыт с причиной](https://github.com/ivbbest/ai-for-developers-project-387/issues/6) |
| Р3 | принята как мониторинг | см. вопрос 2 выше |
| Р4 | выполнена | гейт `git status --porcelain` после шага агента — PR #32 → main `7d3d109` |
| Р5 | выполнена | текст guard'а самодостаточен — PR #32 → main `7d3d109` |
| Р6 | выполнена | правило языка ответов в файле правил (читается раннером) — PR #32 → main `7d3d109` |
| Р7 | выполнена | `issues: read` убран из opencode-review — PR #32 → main `7d3d109` |
| Р8 | принято как известное ограничение модели | правки нет |
| Р9 | **отклонена с причиной**: `AGENTS.md` — публичный артефакт курса (user-story: «Репозиторий… берётся целиком: … и AGENTS.md. Агент в Actions читает его»), ссылка на него в публичном документе корректна; внутренняя инфраструктура — `.agents/`, её ссылок в доке нет | — |