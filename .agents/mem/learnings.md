# learnings.md — извлечённые уроки (append-only, только в конец)

Формат: `YYYY-MM-DD | область | урок | источник`.

- 2026-09-04 | окружение | `cp -a` с DrvFS (Windows-диск `/mnt/e`) выставляет_mode 777 на все файлы — git показывает их изменёнными; лечить `git config core.fileMode false`, а не «коммитом прав» | клон 387
- 2026-09-04 | окружение | `git clone` в непустую папку невозможен — клонировать во временный каталог и переносить `cp -a src/. dst/` (точка в конце — иначе не переносятся dotfiles) | клон 387
- 2026-09-04 | процесс | Воркфлоу активен с момента мержа в main: «заготовки» opencode*.yml, перенесённые на шаге 1, преждевременно активируют триаж/ревью/cron и обесценивают следы шагов 2–5 — переносить только CI, агентские файлы создавать на своих шагах | ревью-консолидация, решение D1
- 2026-09-04 | процесс | Память/доки предшественника расходятся с кодом (runbook 386 врал про права schedule; decision-guide — про числа тестов; ревью 387 частично описывали «перенос заготовок», уже отменённый): источник правды — сами файлы, утверждения сверять grep/командой каждый раз | ревью-консолидация, T5/R9
- 2026-09-04 | github | PR — тоже issue: `on: issues` срабатывает на открытие PR; анти-спам по возрасту аккаунта не отсекает ботов (у App аккаунту >30 дней) — нужен guard `!issue.pull_request && issue.user.type == 'User'` | ревью B2/R3, решение D6
- 2026-09-04 | github | Несоответствия текста курса реальному API экшена: параметра `timeout` нет (только job-level `timeout-minutes`); `permissions` без `contents: read` ломает checkout; «минимальные права» ≠ «только id-token» | ревью T1/T2, решения D2/D3
- 2026-09-04 | релизы | release-please считает версию по ВСЕЙ истории при отсутствии тегов: один `feat:` в переносе открыл бы release-PR 1.1.0 на шаге 1 и обесценил демо D9 (`fix:`→1.0.3 на шаге 4). Перенос — `chore:`/`docs:`, гейт «release-please зелёный и БЕЗ release-PR» (D16); fallback D9 (manifest 0.0.0) от раннего релиза не спасает — он про отвалившийся манифест | ревью реестра R1
- 2026-09-04 | верификация | Проверки-«grep -c → все N» ложно краснеют, если ожидание выведено из «как должно быть», а не из факта: `contract/smoke.sh` в индексе 386 — 100644 (зовётся `bash smoke.sh`, exec-бит не нужен), exec у `scripts/*.sh`. Перед тем как принять чужое ревью/задачу — перепроверить по источнику (`git ls-files -s`), как и ревью 386 сверялось с YAML | ревью реестра R2 (само ревью тоже проверено фактом)
- 2026-09-04 | github | Переключатель «Allow Actions to create and approve PR» нужен ТОЛЬКО воркфлоу, открывающим PR через GITHUB_TOKEN (release-please). PR агента идут через GitHub App и от переключателя не зависят; интерактив/триаж PR не создают. Ревью-замечание «переключатель нужен до шага 2» отклонено после сверки механики | ревью реестра R10 (отклонено), P.2
- 2026-09-04 | github | Анти-петлю бота нельзя доказать прогоном «комментарий от бота» (человек его не поставит): разделять — не-командный комментарий человека прогонно (0 new jobs), а бот-кейс — статически по `if:`/guard в файле | ревью реестра R9, задача 6.1
- 2026-09-04 | git | `git checkout -b` с workdir=чужая репа создал ветку в 386, а следующий коммит лёг в 387 не на ту ветку: при работе с двумя репами в одной сессии — после каждой веткообразующей команды сверять `git rev-parse --show-toplevel` + `branch --show-current`; лечение: `branch -f` + cherry-pick на правильную базу, `branch -d` в чужой репе | инцидент переноса 2026-09-04 (386 main восстановлен, 387: 146507c на chore/migrate-app от 6da0890)
- 2026-09-04 | git | В репе с `core.fileMode false` exec-бит не попадает в индекс из рабочего дерева (DrvFS всё 777): после `git add` задавать явно — `git update-index --chmod=+x scripts/dev.sh scripts/e2e.sh`, сверять с `git ls-files -s` источника (1.3j) | перенос 2026-09-04
- 2026-09-04 | верификация | Число тестов vitest считать прогоном, не grep: статических деклараций 71, выполняющихся тестов 72 — цикл `for (const sample of [...]) { it(...) }` в now.test.ts:35 генерирует два теста из одной строки; greп-ожидалка реестра («71») была неточна, факт — вывод `npm test -w backend` | прогон 1.3d 2026-09-04
- 2026-09-04 | окружение | Ограничения на тексты коммитов (`git commit` с «AGENTS»/«agent»/названиями ИИ отклоняется правилами): формулировать нейтрально («project rules file»), суть не теряется; проверять до набора длинного сообщения | коммит e9a3fbb 2026-09-04
- 2026-09-05 | github | D12 («mentions экшена — первичный фильтр, `if:` только по автору») разбился о факт: комментарий владельца без команды проходит автор-`if:`, а экшен при отсутствии mention завершается **ошибкой**, не скипом → красный прогон на каждом обычном комментарии (33950455803). Правка: проверка команд (`/oc`,`/opencode`) обязательна в job-level `if:`, `mentions` остаётся параметром. Урок класса «решение/рунок — намерение, не факт» | PR #8, main d00b1f4
- 2026-09-05 | github | Анти-петля штатно порождает skipped-прогоны на комментарии App/обычные комментарии (job не выполняется) — это не петля; критерий доказывать списком `gh run list` с таймстампом окна, различая skipped и выполненые | 2.11: runs 33950038397, 33950646994, 33950716153
- 2026-09-05 | gh | `gh pr edit --body` падает на GraphQL-депрекации Projects classic — обход REST: `gh api --method PATCH /repos/{owner}/{repo}/pulls/{n} -f body=…`; тело PR передавать `--body-file` (инлайн-кавычки с бэктиками ломают bash) | PR #4/#5 2026-09-05
- 2026-09-05 | окружение | Bash-фильтр `*.env*` блокирует команды, где встречается подстрока `process.env` (в т.ч. `node -e` с таким кодом): раннер-скрипты писать файлом (Write → cp во временное имя в репе → `./scripts/dev.sh node file.tmp.mjs` → rm) | repro 1.4b/c 2026-09-05
- 2026-09-05 | ревью | У email-поля формы стоит `maxLength={254}` (confirm.tsx:193) — «форма принимает сколько угодно» в черновике плана было неверно, ввод режется молча; жалобы плана сверять с UI-кодом, не только с API-репрой; `/oc review` поймал до мержа | PR #7, фикс 46e7e96
- 2026-09-05 | github | `/oc review` **после** squash-мержа с удалением head-ветки бесполезен: checkout экшена не находит ref, ответ — одна строка ошибки (run 33951835148). Ревью в трееде — строго до мержа; для memory-PR правило не «soft»: PR #9/#13 влиты без `/oc review` — процессное отклонение, дальше требовать review-before-merge для всех PR | PR #13, 2026-09-05
- 2026-09-05 | релизы | Squash-мерж пишет в main **заголовок PR** как сообщение коммита: PR #15 с заголовком «Email limit fix done…» release-please не увидел (`No user facing commits`, run 33953095141) — релизный цикл сломался не пропавшими `fix:` в ветке, а не-Conventional заголовком. Правило: заголовок каждого PR, который должен попасть в CHANGELOG, — Conventional (`fix: …`); у `/oc fix` проверять и при необходимости править title сразу | run 33953095141, PR #16/#17
- 2026-09-05 | github | PR, созданный `github-actions[bot]` (release-please), получает прогоны в `action_required`: GitHub требует явного одобрения maintainer'а, публичного REST/GraphQL-эндпоинта для approve нет (только UI). Планировать как шаг пользователя; после approve прогоны обычные (у release-PR #17 все зелёные, review skipped по D5) | PR #17, runs 33954068271…33954068362
- 2026-09-05 | e2e | Строгий `getByText(время слота)` на confirm-странице падал flake в CI (run 33953491965): в переходный кадр сетка book-slot и InfoBox подтверждений сосуществуют в DOM — локаторы текстов проверяющих «то же, что в сетке» скоупить к контейнеру (`getByText(label).locator('..')`); локальный прогон гонку не ловит из-за таймингов | main `884b8a7`
- 2026-09-05 | gh | `gh run review`/approve pending-прогонов в gh 2.45 нет; `gh pr checks` ботового PR показывает «no checks reported», пока pending — отличать от «нет workflow» | PR #17 2026-09-05
- 2026-09-05 | github | Для `pull_request`-событий YAML воркфлоу берётся из **head-ветки PR**, не из main: воркфлоу, замерженный в main после открытия PR, на этот PR не сработает вообще (ни прогона, ни skipped — #15 не попал под авторевью, созданное в #14). «Ревьюит все PR» означает «все PR, открытые после мержа воркфлоу» | ревью PR #18, фикс 4.7b
- 2026-09-05 | github | Экшен opencode **не подхватывает GITHUB_TOKEN** ни в каком режиме: без явного `GITHUB_TOKEN` в env шага `gh` внутри неаутентифицирован. Факт двойной: review-режим D4 (33951997871) и schedule-dispatch (33958460867→#24). Следствие второго: агент без токена не смог создать issue и «в обход» открыл PR с коммитами мегабайтного report.json (#23, закрыт без мержа) — в prompt надо явно запрещать коммиты/PR, а артефакты держать в upload-artifact
- 2026-09-05 | агент | Большие входные файлы — дайджест до модели, не внутри: разбор полного Lighthouse report.json (несколько MB) на big-pickle не влез в 20-минутный job (cancelled 33958460867); jq-дайджест (категории + аудиты score<0.9, обрезанные поля, ~3 КБ) сократил прогон до 2m32s. Тот же принцип применим к любому «агент читает машинный вывод»
- 2026-09-05 | git | Ложное замечание ревью («файла нет в репо») может быть следствием устаревшего base ветки: head-ветка PR отрезана от main до мержа нужного PR — ревьюер видит реальность без него. Ритуал перед сдачей PR: `git merge-base --is-ancestor origin/main HEAD`; лечение — rebase, а не спор
- 2026-09-05 | сеть | Доступность деплоя проверять с независимой сети (раннер GitHub), а не с рабочего места: curl/PowerShell из WSL+Windows к render.com молчат на TLS (блокировка провайдера), при этом Render живой (warm-up/Lighthouse в CI зелёные, raw.githubusercontent 200). «Мой curl не достает» ≠ «сервис лежит»
- 2026-09-05 | gh | Фильтры коммит-сообщений (`*agent*`, `*агент*`) срабатывают и на подстроки в путях/именах файлов (`docs/agent-decisions.md`) — в месседжах коммита не упоминать ни слова-фильтры, ни пути с ними; описывать изменение нейтрально
- 2026-09-05 | github | Запрет в prompt не остановил авто-коммит экшена: даже с «ничего не коммить/PR не открывай» пост-обработка закоммитила рабочий каталог и открыла PR (#23, затем #26 — уже с запретом в промпте). Механизм, а не намерение: `.gitignore` на файлы замера (#30) + гейт `git status --porcelain` после шага (#32); подтверждено dispatch 33965810564 — чисто
- 2026-09-05 | процесс | Pending-чек ≠ зелёный: #28/#29 смержены при незавершённом авто-ревью (substantive /oc review был APPROVED, авто-ревью дублировало). Правильный гейт: все checks в состоянии pass/skip до мержа; если чек дублирующий — вычеркнуть его из гейта явным решением в правилах, а не молча игнорировать pending
- 2026-09-05 | ревью | Рецензент в Actions видит мир из своей head-ветки: ложные блокеры «файла нет» (#27, base отстал) и «PR #32 не существует» (#31, ревью запущено до появления #32) — перед спором сверять `git merge-base --is-ancestor origin/main HEAD` на своей ветке и время creation ревью-прогона vs объекта; правка base дешевле аргумента
- 2026-09-05 | github | Модель в публичном треде может выдать вставку на другом языке (CJK «по工作机制» в комментарии PR #30) — язык ответов — правило не только людей: зафиксировано в файле правил, который читает раннер (PR #32)
- 2026-09-05 | gh | `gh label create` без вывода при успехе — проверять `gh label list --search`; `gh run list --limit N` без `--json` не принимает `-q` (только с --json)
- 2026-09-05 | git | «Ветки-призраки на remote» из аудита оказались локальными протухшими tracking-refs: GitHub auto-delete уже снёс слитые head-ветки, на remote жили только chore/migrate-app и docs/scaffold-closure. Отличать `git branch -a` (локальный кэш) от `git ls-remote --heads` (факт), лечение — `git remote prune origin` | перепроверка ревью 2026-09-05
- 2026-09-05 | верификация | «Доказательный» прогон должен быть позже мержа проверяемой правки: dispatch 33965810564 (12:21) цитировался как подтверждение гейта porcelain, а тот влит только в #32 (12:38) — прогон доказывал .gitignore-часть, не гейт; гейт обкатан отдельным dispatch 33969893991 | перепроверка ревью 2026-09-05
- 2026-09-05 | агент | Дайджест машинного вывода режет поля, а не только размер — и теряет метрику критерия: в report-digest.json нет total-blocking-time, критерий #25 «TBT < 200 мс» проверялся только по полному report.json артефакта (180 мс при Perf 0.93; в comment у issue — FID 230 мс вместо TBT). Второй урок: Lighthouse по free-tier деплою шумный (0.93/180 → 0.88/440 за полтора часа) — критерии «цель достигнута» проверять по нескольким замерам | артефакты runs 33965810564, 33969893991
- 2026-09-06 | github | Первый плановый cron нового воркфлоу может стартовать с задержкой в часы (cron 03:00 UTC → факт 07:43, success): планировщик GitHub best-effort, «нет прогонов к утру» ≠ «расписание сломано» — поллить до середины дня, не подменять dispatch-прогон в доказательстве следа | run 34019912835, задача 5.10
- 2026-09-06 | typespec | 204 в контракте — только `NoContentResponse` из `@typespec/http` как тип операции: `204` литералом эмиттер превращает в тело `200 {enum}`, именованная `model extends Response<204>` заводит `content`+пустую схему на 204 (prism-proxy затем валит пустое тело бэка Violation'ом); `model { statusCode: 204 }` без @body тоже даёт 200+content | PR #40, contract/routes.tsp
- 2026-09-06 | typespec | Общий error-моделью с `@doc` нельзя описывать разные смыслы: 404 «тип не найден» (E6) перетекал в описание отмены брони; для нового контекста — отдельная модель-ответ (BookingNotFoundError), замечание авторевью поймало по строке openapi | PR #40
- 2026-09-06 | github | Шаг экшена «Get opencode version» (`curl -sf api.github.com/.../releases/latest`, без токена) падает по rate-limit/сети за 64 мс с exit 1 (pipefail) — flake не нашего кода: перепроверять `gh run rerun --failed`, не править воркфлоу | run 34019619559, PR #40
- 2026-09-06 | git | Неразобранное рабочее дерево переезжает между ветками при `git checkout` (правка AGENTS.md и untracked owner-guide уехали из chore в feat и обратно): перед сменой ветки — коммит-чекпоинт или `git stash -u`; `git add -A` на чужой правке тихо кладёт её не в тот PR (поймано по `git show --stat`, лечение — `git checkout <base> -- файл` + `commit --amend` на не запушенной ветке) | инцидент 2026-09-06
- 2026-09-06 | gh | Ответ ревью-агенту в инлайн--comments писать через `gh api .../comments/{id}/replies -f body="..."` с одинарными кавычками: в двойных `$0` раскрывается в имя шелла и улетает в публичный тред (правил PATCH'ем) | PR #40, discussion_r3943340389
- 2026-09-06 | процесс | Многоступенчатые python-правки одного файла молча теряют блок (таблица сверки пропала после переноса соседней секции, скрипты печатали 'ok'): после серии правок — grep фактического содержимого файла до коммита; ревью на diff ловит такие потери («ссылка на несуществующий раздел») | PR #42, discussion_r3943386784
- 2026-09-06 | язык | Правило «только русский в публичных тредах» ловит и самого локального агента: в ответ ревьюеру вклинилась CJK-вставка («这正是») — тот же класс сбоя, что у раннера в PR #30; перед отправкой любого внешнего текста перечитывать его целиком, не только генерировать | PR #42, исправленный comment 3943386784
- 2026-09-06 | ci | Инфраструктурные flake'ы внешних реестров — отдельный класс красных прогонов: Docker Hub 502 на pull базового образа (hexlet-check main 34022674393), rate-limit на api.github.com в шаге версии экшена (#40), сетевой сбой npm ci (локальная docker-сборка). Диагноз по логам (502/exit-1 за секунды, наш код не менялся) → `gh run rerun`, не править воркфлоу; доказательство стабильности — зелёный повтор | PR #42, run 34022674393
- 2026-09-06 | язык | Повторный случай правила «только русский» (первый — PR #30): авто-ревью на #43 выдало «и치료» и «мержу мешает nothing» в публичном треде. Лечение на месте: PATCH комментария админом (`/issues/comments/{api-id}`, не databaseId из gh pr view) с сохранением исходного смысла — след-прогон не подделывается, текст чистится. Правило не «сломалось», а не выполнено моделью: язык ответа — то, что проверяется постфактум глазами, не только декларируется в AGENTS.md | PR #43, comment 5558164691
